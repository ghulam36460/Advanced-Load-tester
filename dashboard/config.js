// Runtime dashboard config. Override these by serving a different file at the same path.
// Example for Vercel/Pages: set window.API_BASE to your backend URL.
window.API_BASE = window.API_BASE || '';
window.WS_BASE = window.WS_BASE || '';
