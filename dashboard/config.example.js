/* eslint-env browser */
/* global window */
// Example runtime config for the static dashboard
// Copy to dashboard/config.js in your deploy and set your backend URLs
// Use absolute URLs for cross-origin; leave empty to default to window.location.origin
window.API_BASE = ""; // e.g., "https://your-backend.example.com"
window.WS_BASE = "";  // e.g., "https://your-backend.example.com"
